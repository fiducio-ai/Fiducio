# Manual rerun; PRs and main run the same packaging checks through Tests.
name: Package
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  validation:
    uses: ./.github/workflows/validate.yml
